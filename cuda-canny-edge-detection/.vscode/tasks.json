{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CUDA C/C++: build active file",
            "type": "shell",
            "command": "make dbg=1 CURRENT_FILENAME=${fileBasenameNoExtension}",
            "problemMatcher": ["$nvcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
